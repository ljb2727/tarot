# 📁 빌드 디렉토리 변경 완료

## ✅ 변경 사항

Android 빌드 출력을 **C:\build\tarot-android**로 변경했습니다.

### 수정된 파일:
1. `android/build.gradle` - 루트 빌드 디렉토리 변경
2. `android/app/build.gradle` - 앱 모듈 빌드 디렉토리 변경

### 빌드 출력 위치:
```
C:\build\tarot-android\
├── app\
│   └── outputs\
│       └── apk\
│           └── debug\
│               └── app-debug.apk  ← APK 파일 위치
└── ... (기타 빌드 파일)
```

## 🚀 Android Studio에서 진행하기

### 1. 프로젝트 동기화
Android Studio로 돌아가서:
- 프로젝트 우클릭 → **"Sync Project with Gradle Files"**
- 또는 툴바의 🔄 아이콘 클릭

### 2. Clean Build
- **Build → Clean Project** 실행
- 이전 빌드 파일들이 정리됩니다

### 3. Rebuild
- **Build → Rebuild Project** 실행
- 새로운 위치(C:\build)에 빌드됩니다

### 4. APK 빌드
- **Build → Build Bundle(s) / APK(s) → Build APK(s)** 클릭

### 5. APK 확인
빌드 완료 후 알림에서 **"locate"** 클릭하면:
```
C:\build\tarot-android\app\outputs\apk\debug\app-debug.apk
```
위 위치가 열립니다.

## 💾 용량 절약

이제 F 드라이브 대신 C 드라이브를 사용하므로:
- ✅ F 드라이브 용량 절약
- ✅ 빌드 파일이 C:\build에 생성됨
- ✅ Gradle 캐시도 C 드라이브 사용

### 예상 용량 사용량:
- 첫 빌드: 약 200-300MB
- 이후 빌드: 약 50-100MB 추가

## 🧹 빌드 파일 정리 (선택사항)

용량이 필요할 때 빌드 파일을 삭제할 수 있습니다:

```powershell
# 전체 빌드 폴더 삭제
Remove-Item -Path "C:\build\tarot-android" -Recurse -Force

# 또는 Android Studio에서
# Build → Clean Project
```

## 📝 참고사항

### 원래 위치로 되돌리기
만약 나중에 F 드라이브로 되돌리고 싶다면:
1. `android/build.gradle`과 `android/app/build.gradle`에서 `buildDir` 라인 삭제
2. Sync Project with Gradle Files
3. Clean & Rebuild

### 빌드 속도
- C 드라이브가 SSD라면: 빌드 속도 동일하거나 더 빠름
- HDD라면: 약간 느릴 수 있음

---

**Android Studio로 돌아가서 Sync → Clean → Rebuild → Build APK 순서로 진행하세요!** 🚀
